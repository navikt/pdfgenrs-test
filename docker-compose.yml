services:
  pdfgenrs:
    image: ghcr.io/navikt/pdfgenrs:1.0.2
    platform: linux/amd64
    ports:
      - "8080:8080"
    environment:
      - DEV_MODE=true
    volumes:
      - ./templates:/app/templates
      - ./data:/app/data
      - ./fonts:/app/fonts
      - ./resources:/app/resources
